apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - install.yaml

patches:
  - path: argocd-server-insecure-patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: argocd-server